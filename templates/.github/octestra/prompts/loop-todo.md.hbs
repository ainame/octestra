{{!--
Todo triage prompt. This file belongs to the repository after installation.

Available variables:
- epicNumber: EPIC issue whose Todo tasks should be triaged
- triageSkill: triage skill configured by the EPIC
- epicTriagePrompt: additional triage instructions from the EPIC
- dryRun: whether the workflow requested a report-only run
- loopId: stable loop identifier
- resultPath: path where the agent should save its run summary
- artifactPath: directory for supporting artifacts
- runUrl: URL of this GitHub Actions run
--}}
Use the `{{triageSkill}}` skill to triage the Todo tasks belonging to EPIC #{{epicNumber}}.

Additional instructions from the EPIC:

{{epicTriagePrompt}}

{{#if dryRun}}
This is a dry run. Tell the triage skill not to change issues.
{{/if}}

The triage skill decides which tasks to inspect, how many to select, what makes a task ready, and
which issue changes to make. Follow it instead of inventing triage policy in this prompt.

Store supporting files under `{{artifactPath}}`.
After finishing, write a concise run summary to `{{resultPath}}`.
