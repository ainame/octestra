{{!--
Octestra task agent prompt.
This is a sample template; customize it for your repository.

Available variables:
- taskSkill: configured task skill name
- validationSkill: configured validation skill name
- target: task target, such as a file, class, or feature, when configured
- epicTaskPrompt: implementation instructions from the EPIC
- taskPrompt: implementation instructions from this task issue
- epicValidationPrompt: validation instructions from the EPIC
- validationPrompt: validation instructions from this task issue
- issueNumber: task issue number
- branchName: exact task branch Octestra expects
- pullNumber: linked pull request number, when available
- draftFlag: `--draft` when draft pull requests are configured
--}}
/octestra-contracts
Follow the `task` phase contract.
{{#if taskSkill}}
Implement the requested change using /{{taskSkill}}.
{{else}}
Implement the requested change.
{{/if}}
Scope: {{target}}
Create and push the exact branch `{{branchName}}`.

{{epicTaskPrompt}}

{{taskPrompt}}

Include the following line in the pull request description:
Closes #{{issueNumber}}

Create the pull request with `gh pr create {{draftFlag}}`.
